\documentclass[border=10pt]{standalone}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{arrows.meta} % Explicitly load library for arrow tips
\usepackage{amsmath,amssymb,amsthm}

\begin{document}
\begin{tikzpicture}
	% --- Pre-calculate all values for maximum robustness ---
	\pgfmathsetmacro{\a}{1.5}          % x-coordinate of point p
	\pgfmathsetmacro{\ay}{\a^2}        % y-coordinate of point p
	\pgfmathsetmacro{\slope}{2*\a}     % Slope (derivative) at p
	\pgfmathsetmacro{\dx}{1}           % Vector x-component
	\pgfmathsetmacro{\dy}{\slope * \dx} % Vector y-component
	\pgfmathsetmacro{\endx}{\a + \dx}   % Vector end-point x
	\pgfmathsetmacro{\endy}{\ay + \dy}   % Vector end-point y
	\pgfmathsetmacro{\labelx}{3}       % x-pos for the T_pC label
	\pgfmathsetmacro{\labely}{\ay+\slope*(\labelx-\a)} % y-pos for the T_pC label
	
	\begin{axis}[
		axis lines=center,
%		xlabel=$x$,ylabel=$y$,
		axis line style={opacity=0},
		xtick=\empty,ytick=\empty,
		%	x axis line style={opacity=.2}, 
		%	y axis line style={opacity=.2}, 
		legend pos=north west,
		xmin=0, xmax=6,
		ymin=0, ymax=6,
		axis equal, % Ensures slopes are visually correct
		clip=false,
		]
		
		% --- Plot the curve y=x^2 ---
		\addplot[domain=0:2.5, samples=100, thick, red] {x^2};
%		\addlegendentry{$C: y=x^2$};
		\node[right, red] at (axis cs: 2.5, {2.5^2}) {$C$};
		
		% --- Plot the tangent line at x=a ---
		\addplot[domain=.5:2.5, samples=100, thick, blue] {\ay + \slope*(x-\a)};
			\node[right, blue] at (axis cs: 2.5, 5.25) {$T_{p}C$};
		
		% Use pre-calculated coordinates for the label
		%	\node[above, rotate=atan(\slope), black!60] at (axis cs: \labelx, \labely) {$T_pC$};
		%	
		
		% --- Draw the tangent vector v=<dx,dy> ---
%		\draw[
%		-{Stealth}, % Arrow tip from arrows.meta library
%		thick,
%		cyan,
%		] (axis cs: \a, \ay) -- (axis cs: \endx, \endy)
%		node[right] {$\mathbf{v} = \begin{pmatrix}
%				1\\ 2x
%			\end{pmatrix}_{p}=\begin{pmatrix}
%				1\\3
%			\end{pmatrix}$};	
		%	\draw[dashed, cyan] (1,3) node[above] {$1$} -- (2,3);
		%	\draw[dashed, cyan] (\a,\ay) to (0,\ay) node[left] {$1^2$};
		
		% --- Mark the point p ---
		\node[circle, fill=magenta, draw=black, inner sep=1.5pt, label={right:$\color{magenta}p$}] at (axis cs: \a, \ay) {};
		%	\draw[dashed, magenta, opacity=.2] (\a,\ay) to (\a,0) node[above right] {$1=x(p)$};
		%	\draw[dashed, magenta, opacity=.2] (\a,\ay) to (0,\ay) node[below right] {$1^2=y(p)$};
		
%		\draw[-Stealth] (1.5,2.25) -- ++(3.5,0) node[above left] {$\mathrm{d}x$};
%		\draw[-Stealth] (1.5,2.25) -- ++(0,3.5) node[below right] {$\mathrm{d}y$};
%		\draw[fill=magenta!50] (\a, \ay) circle (2pt) node[left] {$p=(1.5, 2.25)$};
%		\draw[dashed, cyan] (2.5,5.25) -- ++(0,-3) node[below] {$1=\mathrm{d}x(\mathbf{v})$};
%		\draw[dashed, cyan] (2.5,5.25) -- ++(-1,0) node[left] {$2=\mathrm{d}y(\mathbf{v})$};
%		\draw[fill=cyan] (2.5,2.25) circle (1.5pt);
%		\draw[fill=cyan] (1.5,5.25) circle (1.5pt);
%		\draw[fill=cyan!50] (\a+1, \ay+3) circle (2pt);
	\end{axis}
\end{tikzpicture}
\end{document}